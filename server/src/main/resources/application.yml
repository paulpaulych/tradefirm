server:
  port: 8080
  error:
    includeStacktrace=ALWAYS:spring: ALWAYS

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tradefirm
    username: postgres
    password: 111111
    driver-class-name: org.postgresql.Driver

graphql:
  packages: "paulpaulych.tradefirm"

